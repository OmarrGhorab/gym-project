---
name: user-role
description: User is Omar Ghorab, building the Gym Platform Laravel 12 backend solo through sequenced phases with mandatory review-agent gates
metadata:
  type: user
---

User (Omar Ghorab) is the engineer building the Gym Platform REST API backend (Laravel 12 / PHP 8.4, MySQL, Redis, Queues). Works through sequential phases (`phases/`, `specs/`) where each phase has numbered tasks (e.g. T070 = performance review) and a mandatory review-agent workflow defined in CLAUDE.md. Treats the Constitution (`.specify/memory/constitution.md`) as authoritative — expects findings cited against its numbered principles (esp. §VI Performance). Wants real, calibrated findings respecting YAGNI, not speculative ones, for small foundation slices.
